import styled from 'styled-components';

const TodoListItems = styled.div`
  padding: 1rem;
  display: flex;
  align-items: center;
`;

const TodoListItem = () => {
  return (
    <TodoListItems>
      <div></div>
    </TodoListItems>
  );
};

export default TodoListItem;
